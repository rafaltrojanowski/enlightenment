
.login-panel.panel.panel-default
  .panel-heading
    %h3.panel-title Forgot your password?
  .panel-body
    = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, html: { class: '', role: 'form'}) do |f|
      %fieldset
        = f.label 'Email'
        = f.input_field :email, required: true, autofocus: true, class: 'form-control input-sm'
        .form-actions
          = f.button :submit, "Send me reset password instructions", class: 'a btn btn-success'
    = render "devise/shared/links"