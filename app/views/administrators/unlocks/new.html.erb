<div class="container login-box">
  <div class="padded">
    <div class="login box" style="margin-top: 80px;">

      <div class="box-header">
        <span class="title"><%= t('administrators.auth.resend_unlock_instructions', default: "Resend unlock instructions") %></span>
      </div>
      <div class="box-content padded">
        <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "separate-sections" }) do |f| %>
          <%= f.error_notification %>
          <%= f.full_error :unlock_token %>

          <%= f.full_error :email %>
          <div class="input-group">
            <span class="input-group-addon" href="#">
              <%= draw_icon 'envelope' %>
            </span>
            <%= f.email_field :email, autofocus: true, placeholder: t('administrators.auth.placeholders.email', default: "Email"), class: 'form-control'%>
          </div>

          <div>
            <button type="submit" class="btn btn-blue btn-block btn-default">
              <%= t('administrators.auth.resend_unlock_instructions_button', default: "Resend unlock instructions") %>
              <%= draw_icon "ok" %>
            </button>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>